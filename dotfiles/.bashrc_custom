# TODO Maybe make this a directory with separate files to break everything out into pieces

# Aliases that I use all the time 
alias vim=nvim
alias n=nvim
alias ls="ls --color=auto"

# custom functions

